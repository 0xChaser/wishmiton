nelmio_api_doc:
    documentation:
        info:
            title: WishMiton
            description: This is like marmiton but on Wish and it's only the API ! 
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        path_patterns:
            - ^/api(?!/doc$)